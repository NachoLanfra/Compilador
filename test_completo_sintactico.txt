programa_test

ushortint a, b, c;
doublef x, y, z;

typedef pares = [2$us, 4$us, 6$us, 8$us];

class persona
begin
    ushortint edad;
    doublef altura;
end;

class empleado
begin
    ushortint sueldo;
    extends persona;
end;

persona p;
empleado e;

doublef function calcular(ushortint p, doublef q)
    ushortint r; 
begin
    r := p;
    ret(r);
    ret(q);
end;

{{ -------- ERRORES DE DECLARACION -------- }}

ushortint;
doublef ;
ushortint a b;
doublef x,;


begin

    a := 10$us;
    b := 20$us;
    c := a + b * 2$us;
    x := 10.5;
    y := x + 2.5;
    z := -10.5;

    pout('Hola mundo');
    pout(a + b * 2$us);

    calcular(a, x);
    a := calcular(a, x);

    a := b = (2$us);
    x := y = (3.5);

    a := b = (c + d);
    x := y = (2.5) + z;

    p.edad = 20$us;
    p[0$us] = 25$us;
    p[a] = 30$us;

    if (a > b)
        a := b;
    end_if;

    if (a > b)
        a := b;
    else
        b := a;
    end_if;

    repeat
        a := a + 1$us;
    while (a < 100$us);

    calcular(a, x)[1$us, 2$us];
    calcular(a, x);

    ret(a);

    {{ -------- ERRORES DE ASIGNACION -------- }}

    a := ;
    a b := 10$us;
    a := 10$us

    {{ -------- ERRORES DE ATRIBUTOS -------- }}

    p.edad = ;
    p[ = 10$us;
    p[] = 10$us;

    {{ -------- ERRORES DE POUT -------- }}

    pout();
    pout(;
    pout(a + );

    {{ -------- ERRORES DE RET -------- }}

    ret();
    ret(;
    ret(a + );

    {{ -------- ERRORES DE IF -------- }}

    if (a > b
        a := b;
    end_if;

    if a > b)
        a := b;
    end_if;

    if (a >)
        a := b;
    end_if;

    if (a > b)
        a := b;
	end_if;
    {{ -------- ERRORES DE REPEAT-WHILE -------- }}

    repeat
        a := a + 1$us;
    while (a < 100$us;

    repeat
        a := a + 1$us;
    (a < 100$us);

    {{ -------- ERRORES DE INVOCACION -------- }}

    calcular(a,);
    calcular(;
    calcular(a x);

    {{ -------- ERRORES DE LISTA DE CONSTANTES -------- }}

    calcular(a, x)[];
    calcular(a, x)[1$us,];
    calcular(a, x)[,1$us];

    {{ -------- ERRORES DE EXPRESIONES -------- }}

    a := b + ;
    a := * b;
    a := b / ;
    a := (b + c);

    {{ -------- CONSTANTES NEGATIVAS -------- }}

    z := -10.5;
    z := -0.5;
    a := -10$us;

    {{ -------- RECUPERACION -------- }}

    a := ;
    a := 30$us;
    pout('La compilacion continua');
    b := a;

end;
